## Summary

<!-- One or two sentences describing what this PR does. -->

Closes #

## Changes

<!-- Brief bullet list of what changed and why. Not a file-by-file diff — a human-readable summary. -->

- 

## Checklist

- [ ] Follows naming conventions defined in `GEMINI.md`
- [ ] No `any` types introduced
- [ ] `.env.example` updated if new environment variables were added
- [ ] Migrations included if schema was changed
- [ ] Tests added or updated for new logic
- [ ] `turbo run lint` passes
- [ ] `turbo run type-check` passes
- [ ] `turbo run test` passes
- [ ] Terraform files formatted with `terraform fmt` (if infra changed)

## Manual Steps Required

<!-- IMPORTANT: List any manual steps that must be taken outside of this PR for the functionality to work.
This includes things like:
- GitHub Actions secrets that need adding (name, where to get the value)
- AWS resources that need creating manually (e.g. bootstrapping state buckets before Terraform can run)
- Third-party accounts or projects that need setting up (e.g. Vercel project linking, Expo project creation)
- Environment variables to add to Vercel, EAS, or other platforms
- One-time CLI commands to run locally

If there are no manual steps, write "None".

Format each step clearly so it can be followed without context: -->

**None** <!-- Remove this and replace with steps if applicable -->

## Notes for Reviewer

<!-- Anything the reviewer should know: tradeoffs made, known limitations, follow-up issues already logged. -->
