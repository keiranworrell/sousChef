# pnpm settings
# Allow packages to use their own React version (web=19, mobile=18)
strict-peer-dependencies=false

# Hoist all packages to root node_modules so Metro bundler can resolve them
shamefully-hoist=true
