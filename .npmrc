# Expo 52 requires React 18, apps/web uses React 19.
# legacy-peer-deps prevents npm from erroring on the peer dep conflict
# between these two React versions in the same workspace.
legacy-peer-deps=true
