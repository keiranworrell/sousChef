# pnpm settings

# Both apps are on React 19 since the Expo 57 upgrade, so this is no longer
# papering over two React majors. Kept because Amplify's peer ranges are
# untidy and strict resolution fails the install on warnings we accept.
strict-peer-dependencies=false
