import { Text, View } from "react-native";

export default function Index(): JSX.Element {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text style={{ fontSize: 32, fontWeight: "bold" }}>sousChef</Text>
    </View>
  );
}
